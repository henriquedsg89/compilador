programa @PROG;

const CONT_INT = 20;
const CONT_LIT = 'shuwbaca';

var X, Y: inteiro;
var C: cadeia[5];

proc borala(val param1: inteiro; ref param2: inteiro);
{
	escreva(param1, param2)
};
funcao vemca(val param1,param2: inteiro): inteiro; var in_x: inteiro; {
    vemca := param1 + param2;
};

{
	se nao verdadeiro entao
		leia( X )
	senao
		escreva( vemca(1,5) );

	borala(3,X)
}.